version: '3.8'

services:
  bot:
    build: .
    container_name: freelance_bot
    restart: unless-stopped
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - DATABASE_URL=sqlite:///data/freelance_bot.db
